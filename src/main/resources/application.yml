spring:
  application:
    name: mechugi.io
  datasource:
    url: jdbc:h2:mem:mechugi
    driver-class-name: org.h2.Driver
    username: sa
    password:
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: create-drop

server:
  shutdown: graceful
